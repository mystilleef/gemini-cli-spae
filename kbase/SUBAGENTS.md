# Agent

- Strictly observe `agent-efficiency-protocol.md` in `kb`.
- Edit the same file sequentially across turns.
- Prefix all shell commands with `rtk`.
- If available, use `exa mcp` tools for web research and fetches.
- Use `vibe check mcp` tools to scrutinize complex ideas, plans, or
  solutions.
- Never log, print, or commit secrets, API keys, or sensitive data.
